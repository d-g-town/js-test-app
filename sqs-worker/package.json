{
  "name": "sqs-keda-test-worker",
  "version": "1.0.0",
  "description": "SQS worker for testing KEDA autoscaling",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "produce": "node producer.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.600.0",
    "express": "^4.17.1"
  }
}
